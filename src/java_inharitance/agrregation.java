package java_inharitance;

public class agrregation {
    // aggregation is process that be use to one class aquire anather class proprty.
    // whithout using is a relationship
    // one class access property off anther class use create object off the class .
    // means one object has part off annther object => has relation ship
    // example is engine has part off bike

    int num=101;

    void dis()
    {
        System.out.println(num);
    }
}
